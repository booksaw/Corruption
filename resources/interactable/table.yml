images:
- table:10:1
# format: file (DO NOT INCLUDE .png):priority:scale

interact:
- checkpoint

#options:
#  stopMove: t/f
#  mode: (interact, collide)
#  scale: (default scale in editor)

# Options include
# - kill (kills player)
# - spawn (moves player to spawn)
# - tp:x:y (moves player to that location)
# - relativetp:mode:x:y (moves player to that percentage of the screen, mode - centre, corner
# - direction:(left, right)
# - time:(stop, start) 
# - delay:(amount millis):ref
# - finish (ends the level)
# - checkpoint (when the player dies they go back to there)

